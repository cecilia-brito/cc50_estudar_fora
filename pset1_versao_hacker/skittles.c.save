
#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <cs50.h>
#include <time.h>

int
main(void){
	srand(time(NULL));
	int randomNumber = rand() % 1024;
	int userNumber =  0;
	printf("o valor é:%d',randomNumber");
	while( userNumber != randomNumber ){
		printf("Adivinhe o número entre 1 e 1023, humano :)");
		int userNumber = get_int("Digite seu chute aqui:", userNumber);
		if(userNumber < randomNumber){
			printf("O número que você chutou é muito baixo!\n");
		} else if (userNumber > randomNumber){
			printf("O número que você chutou é muito alto!\n");
		}
	}
	printf("Parabéns humano!\n");
}
